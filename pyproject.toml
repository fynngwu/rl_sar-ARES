[project]
name = "ares-tools"
version = "0.1.0"
description = "ARES robot tools — camera solver, auto pilot, etc."
requires-python = ">=3.10,<4"
dependencies = [
    "opencv-python>=4.8",
    "requests>=2.31",
    "pyyaml>=6.0",
    "numpy<2",
]
